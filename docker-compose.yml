services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - sqlite-data:/app/data
      - uploads:/app/public/uploads

volumes:
  sqlite-data:
  uploads:
