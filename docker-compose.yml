services:
  nuola-energy-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nuola-energy-dashboard
    restart: unless-stopped
    ports:
      # Nur auf localhost binden: die App ist ausschliesslich ueber den
      # vorgelagerten nginx erreichbar, nicht direkt aus dem Internet/LAN.
      # (Falls nginx auf einem ANDEREN Host laeuft: auf die interne IP
      # des nginx-Hosts einschraenken statt 127.0.0.1.)
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
    volumes:
      - nuola-energy-dashboard-data:/app/data
      - nuola-energy-dashboard-uploads:/app/public/uploads

volumes:
  nuola-energy-dashboard-data:
  nuola-energy-dashboard-uploads:
